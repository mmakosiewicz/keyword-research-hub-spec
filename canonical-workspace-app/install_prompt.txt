{{MANIFEST_SETUP}}

Install the files in `canonical-workspace-app/` VERBATIM into the matching Console application/template locations. This directory is the canonical implementation.

DO NOT recreate the app from the Markdown workflow specifications.
DO NOT generate substitute HTML, navigation, tabs, schemas, or Flask routes.
DO NOT seed a target domain, Rank Tracker project ID, tags, keyword bank, tier file, member name, or example customer data.

After installation:
1. Confirm `/applications/letaido_keyword_research_hub/` returns HTTP 200.
2. Open the app in Whiteboard.
3. A fresh database must open the gated four-step setup wizard before the app shell. Verify: Site & product → optional research sources → core via Rank Tracker/CSV/paste → spend review and explicit approval.
4. Do not fill the wizard on the user's behalf or start model spend without their approval.
5. After completion, verify the professional light product header, five workflow tabs, Manage tiers, and Master List Table / Topic map toggle.
6. Request/confirm the manifest's Console connector approvals if the user chooses Rank Tracker.
7. Do not copy data from another workspace.
